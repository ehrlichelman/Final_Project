to do:

*add threads to worker class and test update

*add register object. mapping between worker and id.      V
  possible bugs: - collision between identical Id's
  add error detection and handling

*add detach function to publisher

*add abilty for observer to reach a specific worker by id V

*decide between push/pull model

*add logging

